{
  "name": "boishakh-auth",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "swc src -d dist --copy-files --strip-leading-paths",
    "build:types": "tsc --emitDeclarationOnly",
    "build:full": "npm run build && npm run build:types",
    "dev": "swc src -d dist --copy-files --strip-leading-paths --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@swc/cli": "^0.7.7",
    "@swc/core": "^1.11.31",
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}

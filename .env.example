# Environment Variables for Different Environments

## Development Environment (.env.development)
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

## Testing Environment (.env.testing)
NODE_ENV=testing
PORT=3001
LOG_LEVEL=debug
# Set to silent to disable logging during tests
# LOG_LEVEL=silent

## Production Environment (.env.production)
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Google Cloud Platform Configuration (Required for Production)
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Alternative: Use GCP metadata service (when running on GCP)
# GOOGLE_APPLICATION_CREDENTIALS not needed if using GCP metadata service
